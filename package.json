{
  "name": "emma-task",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start:dev": "ts-node-dev --respawn --transpile-only src/",
    "prebuild": "rm -rf dist",
    "build": "tsc -p ./tsconfig.build.json",
    "test": "yarn jest",
    "test:watch": "yarn test --watch",
    "buy-shares": "yarn ts-node scripts/buy-shares"
  },
  "dependencies": {
    "axios": "^0.26.1",
    "chance": "^1.1.8",
    "envalid": "^7.3.0",
    "fastify": "^3.27.4"
  },
  "devDependencies": {
    "@types/chance": "^1.1.3",
    "@types/jest": "^27.4.1",
    "@types/node": "16",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.3",
    "ts-node": "^10.7.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.6.2"
  }
}
